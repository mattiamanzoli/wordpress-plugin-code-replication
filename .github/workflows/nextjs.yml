import type { NextConfig } from "next";

const REPO = "wordpress-plugin-code-replication";

const nextConfig: NextConfig = {
  // Genera HTML/CSS/JS statici in /out
  // (sostituisce il vecchio `next export`)
  output: "export",

  // Pubblica l'app sotto /<repo>
  basePath: `/${REPO}`,

  // Produce /percorso/index.html (compatibile hosting statico)
  trailingSlash: true,

  // Disattiva l'Image Optimization (non disponibile in export statico)
  images: { unoptimized: true },
};

export default nextConfig;
